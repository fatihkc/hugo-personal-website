{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
{{- $pages = $pctx.RegularPages -}}
{{- else -}}
{{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ if eq  .Title  .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Author.email }}
    <managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Author.email }}
    <webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    <atom:link href="{{.Permalink}}" rel="self" type="application/rss+xml"/>
    {{ range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      <description>
        {{- if .Params.featuredImage -}}
        &lt;img src=&quot;{{ printf "%s%s" (strings.TrimSuffix "/" $.Site.BaseURL) .Params.featuredImage }}&quot; alt=&quot;{{ .Title }}&quot; /&gt;&lt;br/&gt;&lt;br/&gt;
        {{- end -}}
        {{- with .Description | html }}{{ . }}{{ else }}{{ .Summary | html }}{{ end -}}
      </description>
      {{- with .Params.featuredImage -}}
      <enclosure url="{{ (printf "%s%s" (strings.TrimSuffix "/" $.Site.BaseURL) .) }}" length="100000" type="image/webp" />
      {{- end -}}
      <content:encoded>{{ (printf "<![CDATA[%s]]>" (.Content | replaceRE `src="(/images/[^"]+)"` (printf `src="%s%s"` (strings.TrimSuffix "/" $.Site.BaseURL) "$1") | replaceRE `\*\*Link to heading\*\*` "" | replaceRE `<span class="sr-only">Link to heading</span>` "" | replaceRE `aria-hidden="true" title="Link to heading"` `aria-hidden="true"`)) | safeHTML }}</content:encoded>
    </item>
    {{ end }}
  </channel>
</rss>
